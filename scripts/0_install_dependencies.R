# Short script to install all dependencies

packages <- c("dplyr", 
              "janitor", 
              "lubridate", 
              "sf", 
              "leaflet", 
              "htmltools")
install.packages(packages)
